---
name: evidence-driven-workflow
description: "通用工程与科研工作流方法论（从三个真实项目的长期实践中提炼：非侵入式推理观测工具、联邦学习论文实验、多机平台部署与排障）。适用于任何需要严谨推进的工作：新项目启动、实验设计与管理、线上问题排查、数据与口径管理、论文写作、发布上线。核心节奏（三段式）：先讨论确定方案 → 小批量执行 → 逐模块逐层次验收，小步循环推进，验收不过就带证据回方案阶段。八条核心纪律：①证据链闭环（先取证再动手，修完再验证）②单一事实源（数字只有一个出处）③隔离与归档（不混、不删、可追溯）④确认式推进（重要操作先授权）⑤自动化验证+回滚（可重复、可恢复）⑥诚实口径（如实披露，经得起追问）⑦知识沉淀（踩坑留痕、方法论化）⑧资源与风险记账（成本/副作用/密钥安全）。触发词：工作流程、工作方法、项目工作法、方法论、如何开展、workflow、working approach、evidence-based、严谨推进、实验管理、排障流程、上线流程。"
metadata:
  version: "1.1.0"
  last_updated: "2026-09-19"
  status: active
  data_access_level: redacted
  task_type: open-ended
  license: MIT
---

# 证据驱动工作流（Evidence-Driven Workflow）

一套从真实项目中提炼的通用工作纪律：**任何结论都要有可复现的证据，任何改动都要先确认后执行，任何知识都要沉淀成资产**。

来源：2026 年夏秋三个并行项目的实践沉淀——
- **推理观测工具**（非侵入式运行时探针）：浮点不确定性根因定位、开销量化、90+ 测试的真机证据链
- **联邦学习论文实验**（30-cell 实验矩阵 + 消融 + 论文）：实验面隔离、口径管理、确认式大纲
- **多机平台部署**（四台服务器、双环境栈、CI 自动部署与回滚）：线上排障、发布链路、端到端验证

这套纪律不绑定特定领域或工具链，适用于科研实验、软件开发、系统运维、论文写作等一切需要"把事情做对"的工作。

## 何时使用

- 用户说"开始 XX 项目 / 帮我管理实验 / 排查这个问题 / 准备上线 / 整理数据"——先按本工作流的步骤走
- 任何**口径**（数字、术语、实验条件）存在歧义的场景
- 任何**操作有副作用**（删数据、改配置、推送上线、花钱调用）的场景

**不触发**：单纯写代码（用常规开发流程）、单纯审稿（用 `paper-audit`）、大纲撰写（用 `paper-outline-confirm`）——本 skill 是这些专用 skill 之上的公共底座，不与它们冲突。

## IRON RULES（八条核心纪律）

1. **证据链闭环**：先取证，再动手，修完再验证。
   现象 → 多源交叉取证（日志/数据库/真机复现）→ 根因 → 最小修复 → 端到端验证 → 留痕。
   包含"反向验证"：声称"没动过 / 没受影响"也要有硬证据（git status、文件 mtime、运行时长），记忆不算数。

2. **单一事实源**：每个数字、每个定义只有一个出处。
   一处定义、处处引用；改口径时先归档旧数据 → 重跑 → 回填所有下游引用（文档/图表/PPT），绝不留下图文不一致。
   幻灯片上的每个数字都必须能在事实源里找到，找不到就非零退出。

3. **隔离与归档**：不混、不删、可追溯。
   不同目的的产物（主实验/消融/调试/候选方案）分目录存放，物理隔离；旧口径整体归档并写弃用说明（弃用理由 + 恢复条件），只移动不删除。

4. **确认式推进**：用户拍板才动手。
   危险操作（上线、强推、删数据、花钱调用、改共享配置）前先给出"建议 + 已核对的前提清单"，等授权；决策逐条记录，方向随时可追溯。

5. **自动化验证 + 可回滚**：任何重复性动作都写进脚本和流水线。
   部署/发布带健康检查和回滚点；测试分三层（单元 → 集成 → 端到端）；CI 变红必须归因到根，修测试基础设施而不是改被测代码来"消红"。

6. **诚实口径**：如实披露，数字经得起追问。
   "最优选择/最终结果"要说清楚口径（是否验证集选模、是否同预算、是否同轮数）；名义值 ≠ 实际值要披露（如名义噪声率 vs 实测翻转率）；负结果同样是证据，写清适用边界。

7. **知识沉淀**：每个坑都不白踩。
   踩坑即记录（原因、表现、修法、如何提前发现）；重复出现的流程提炼成模板/skill；排障方法写成文档（怎么查、别踩什么）；沟通与记录说人话，不堆术语。

8. **资源与风险记账**：动手前先盘算成本与副作用。
   共享资源先看占用（GPU/端口/队列）；探测和调试也会留痕或产生费用，如实记账；密钥不出机器、不进仓库；删任何东西前先清点并留档，删完复核。

## 核心工作流：三段式节奏（先讨论方案 → 小批量执行 → 分层验收）

> 整条工作流的骨架是三个阶段的循环。每个循环只推进一小步：先讨论确定方案，小批量试执行，逐模块逐层次验收；验收不过就带着证据回到阶段 1 调整方案。宁可用三小步走完全程，不用一大步走完再返工。

### 阶段 1：先讨论、确定方案（Plan）

- **先讨论后落盘**：目标、范围、口径定义、证据链（哪些实验/测试支撑哪个结论）、执行顺序，与用户讨论并逐项确认后再写入文件；未确认的内容不落盘
- **方案拆模块**：大方案拆成可独立验收的模块；模块边界、依赖顺序、验收标准在方案里写清
- **单一事实源先行**：方案、实验矩阵、配置成为唯一权威定义，后续所有引用都指向它
- **方案里自带失败预案**：判据（什么算通过、什么算否决）、复测条件、回滚路径在动手前说清

### 阶段 2：小批量执行（Execute small）

- **先小后大**：第一轮只做一小批（几个实验格子 / 一个里程碑 / 一台机器 / 一个模块），跑通验证后再放量；绝不一次全量开工
- **小批量自带完整性**：每一批都可独立验收、可回滚、可隔离（独立结果目录/独立提交/独立环境）
- **试点目的就是找问题**：小批量不是走过场，是验证"实验轴真实生效、机制真的按设计工作"；发现问题就带着证据回到阶段 1
- 单个批次内部的执行细节见下方八步流水线

### 阶段 3：逐模块逐层次验收（Verify by module, by layer）

- **按模块验收**：一个模块完成即验收，不等全部做完；模块验收标准在阶段 1 定好
- **按层次验收**：单元 → 集成 → 端到端；试点 → 全量；测试环境 → 生产；单机 → 多机；每过一层才进下一层
- **验收与结果一并落单一事实源**：矩阵/表格/文档当场更新，验收记录（谁验的、怎么验的）可追溯
- **验收不过就回阶段 1**：带证据回去调整方案，绝不带病推进、不带病引用数据

### 阶段 2 的内部细节：八步执行流水线

单个小批量内部按下面八步走，每步完成后检查该步验收线：

```
0. 开工核对      环境确认（git 状态、共享文件 fetch 对比、待办查重、资源占用）
1. 澄清问题      时间点 / 环境 / 错误原文；不清楚就问，不猜
2. 取证          多源交叉验证、双向坐实；必要时真机复现
3. 最小修改      只改该改的，带测试，走正规路径（API/脚本），不绕过机制
4. 验证          单元 → 集成 → 端到端；否定性断言也要证据
5. 收尾          提交记录证据；清理临时产物；复核影响面
6. 沉淀          memory/文档/skill；#lesson 留痕
7. 汇报          数字可溯源、副作用记账、说人话
```

### 步骤 0：开工核对（防 80% 事故）

- `git status` / `git log`：工作区是否干净、本地是否落后远端；**共享文件**（可能被别人改的文档/工作流/配置）先 fetch 对比差异
- 待办清单查重：做过的事不重复问、不重复跑
- 资源占用：GPU 显存、端口监听、任务队列、磁盘——提交批量任务/起服务前必查
- 明确范围：用户当前只关心什么；其他项目/其他问题不顺手做

### 步骤 1：澄清问题

问题报告含歧义时，向上要三样东西：**环境**（哪台机器/哪个栈/哪个分支）、**时间点**、**错误原文**。
大多数排查时间浪费在"猜错了环境/猜错了时间窗"。宁可多问一句，不要埋头跑偏。

### 步骤 2：取证（证据链）

- 多源交叉：同一结论用 ≥2 个独立来源印证（日志 + 数据库 + 直接探测 + 代码路径）
- 双向坐实：正向复现现象，反向验证假设（"如果根因是 X，那么 Y 应该也……"）
- 前端界面不是证据源：UI 状态会滞后/掩盖，以服务端日志、数据库记录、代码逻辑为准
- 取证过程本身要无害：只读优先；需要真实调用时先声明会产生的副作用（如上游计费）

### 步骤 3：最小修改

- 只修一个根因，不顺手重构、不扩大范围
- 走正规路径：改配置用管理 API/界面，不用裸 SQL 绕过校验、缓存失效与审计
- 每处修改带对应测试；测试先红后绿
- 共享/他人文件：基于最新版本修改，不覆盖他人改动（先同步再改）

### 步骤 4：验证

- 分层验证：单元 → 集成 → 端到端/真机；修复后必须跑通一次**真实路径**（不是模拟）
- 验证宽度与影响面匹配：改了配置校验逻辑，就要验证配置读写两侧
- CI 红 → 归因（是本次引入还是既有问题）；既有问题单独记录，不混入本次改动

### 步骤 5：收尾

- 提交信息包含"证据"（修复了什么、怎么验证的），可与 issue/任务记录互链
- 清理临时文件、探针、调试打印、测试数据
- 复核：本次改动是否影响其他环境/其他分支/其他调用方

### 步骤 6：沉淀

踩坑、决策、环境事实写 memory（当日记录 + 长期条目）；批量出现的方法论提炼成 skill/模板；文档与代码同仓管理，过程叙事（怎么做的、为什么）进任务记录，稳定契约（接口/架构）进正式文档。

### 步骤 7：汇报

- 结论先行，数字可溯源（来源记录/命令/文件路径）
- 副作用如实汇报（包括排查过程本身产生的记录、费用、残留物）
- 说人话：先给大白话解释，再给术语

## 四个高频场景的细则

### 场景 A：实验/数据管理（科研或数据对比）

- **单一事实源**：实验矩阵、结果目录、图表脚本由一处定义（如 `matrix.py`/`collect.py`），`probe_matrix` 类似的状态命令随时可查进度
- **实验面隔离**：主实验 / 消融 / 调参 / 候选方案分独立结果目录，收集脚本互不扫描；不同口径的批次不混存
- **复用基线要记账**：复用别处跑出的格子要写 provenance（来源运行号、配置、时间），并跑预算门校验
- **结果判读纪律**：先验证"实验轴真的进入优化过程"（组间差异是不是被训练架构短路），再读数字
- **重跑定式**：改口径 → 旧批次整体归档（附弃用说明）→ 重跑 → 回填所有下游引用
- **资源预算**：GPU 时间是硬约束；筛选实验（短轮数）先行，确认实验（全长）后上；基线可复用就不重跑
- **贡献判定**：候选方法是否入选要看规定的判据（如带噪格均值为主、干净格掉点超阈值否决），判据代码要能在触发条件走到两个分支（否则复测形同虚设）

### 场景 B：线上排障

- 先问环境/时间/错误原文；先看有没有别人在用同一环境（并发工作流会污染判断）
- 取证链：入口日志 → 业务日志 → 数据库记录 → 直接探测上游/对端 → 代码路径确认；每层找"该层唯一的事实"
- 识别"假象层"：200 状态码可能包着业务错误信封；DB 无痕可能意味着拒绝发生在写库之前——**无记录本身也是证据**
- 修复前后各留快照（配置 before/after、错误计数 before/after），用差异说话
- 排障副产品如实记账（探测产生的审计日志、误建的上游任务、残留的监听端口），修完清理

### 场景 C：发布上线

- 部署链路自动化：push → 构建 → 切换 → 健康检查 → 失败自动回滚；每次部署带显式回滚点标签（如 `prod-prev`）
- 切换语义要精确：只切目标容器（`--no-deps`），不连带重建依赖；只信任本次构建的镜像标签，不信任可能被覆盖的 `latest`
- 上线前核对前提清单（安全组端口、CORS、密钥、镜像可用性）；上线后外部验收（公网 URL、健康端点、版本指纹）
- 生产与测试物理隔离；文档口径与实际状态随时对齐（孤儿容器/孤儿配置是事故温床）
- 版本分发链路（多机/多区域）要可重复：目录收敛、配置生成脚本化、差异只留在"应该不同"的地方

### 场景 D：论文/口径管理

- 大纲/写作类任务用 `paper-outline-confirm` 的确认式流程；数字只从代码与原始结果 JSON 核实，讲实际有的东西
- 口径披露完整：选模规则（验证集选模 ≠ 最终轮）、预算（轮数/学习率是否对等）、名义 vs 实测
- 头条数字只选同预算对比；跨预算、跨场景的数字作趋势不作头条
- 图表与文字同步：改数据必须连带改图、改表、改摘要，提交前做一致性与"是否被引用"检查

## 常见反模式（看见就停）

**破坏三段式节奏的**：

13. 方案没讨论确定就全量开工 → 全量返工（真实案例：方案改动让实验轴在训练里被短路，全量跑完才发现对比表假阴性，整批重来；一次提交 80 个任务把共享 GPU 打 OOM）→ 先定案 + 小批量试点验证机制生效，再放量
14. 全部做完再一次性验收 → 问题埋到最后、带病数据污染下游 → 逐模块逐层次验收，验收不过回阶段 1，绝不带病推进

**其他**：

1. 提交批量任务前不看资源占用 → 全队 OOM / 排队假死
2. 只看任务状态不看日志尾 → 异常被吞后"假成功"
3. 用裸 SQL 改配置 → 绕过校验、缓存失效、审计（之后的调查全部失真）
4. 改共享文件前不 fetch → 把别人的规则/改动一起回退
5. 拿到含糊问题报告直接开跑 → 在错误环境/错误时间窗里空转
6. 用记忆代替硬证据（"应该没变"）→ 没有 git/mtime/日志佐证的否定陈述不作数
7. 改数据只改一处（正文改了图表没改）→ 图文不一致，数字失去信用
8. 合并/上线任务擅自扩大操作（重放、改写历史、顺手重构）→ 范围以外的事先问
9. 把"设计如此"当"实测如此"写进结论 → 如把验证集选模的 checkpoint 说成 final-round
10. 轻信下游/上游的状态透传 → 对方吞掉的错误（如 200 信封）要自己捞
11. 临时产物不清场 → 残留端口/僵尸任务/假痕迹干扰后续判断 → 结束即清理并复核
12. 改测试来"消红" → 覆盖变弱、真回归漏网 → 先归因，修测试基础设施不改断言

完整 14 条与事故对照见 `references/anti-patterns.md`。

## 配套文件

- `references/task-pipeline.md` — 八步流水线逐步骤详解：验收线、检查清单、常用取证命令思路
- `references/anti-patterns.md` — 反模式与真实事故案例对照（每个反模式配一个来源项目的真实教训）
- `templates/` — 可从本项目直接复用的模板（任务记录、实验批次记录、排障快照）